import "./globals.css";

export const metadata = {
  title: "Patient Portal — Highland Valley Medical Care",
  description:
    "Book appointments, view your care team, and manage your visits with Highland Valley Medical Care in Coram, NY.",
  robots: { index: false },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
        <link
          href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
