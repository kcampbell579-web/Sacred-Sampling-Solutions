# Neon Postgres connection string (used when the portal is wired to a real DB).
# Get this from the Neon dashboard → your project → Connection Details.
DATABASE_URL="postgresql://user:password@ep-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require"

# The mockup runs with NO env vars — data lives in the browser (localStorage).
