# Neon Postgres connection string (Project → Connection Details → "pooled" connection).
# Add this in Vercel → Project → Settings → Environment Variables as DATABASE_URL.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require"
